<table>

  <tr>
    <th>Date</th>
    <th>Paid to</th>
    <th>Amount</th>
  </tr>
  <% @account.checks.each do |check| %>
  <tr>
  <td><%= check.date %></td>
  <td class = "name"><%= check.to %></td>
  <td><%= number_to_currency(check.amount) %></td>
  </tr>
  <% end %>
</table>